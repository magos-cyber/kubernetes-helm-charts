prometheus:
  enabled: true
  server:
    persistentVolume:
      size: 10Gi
    retention: 15d

grafana:
  enabled: true
  persistence:
    enabled: true
    size: 2Gi
  adminPassword: changeme  # override via --set or secret
  service:
    type: ClusterIP
    port: 80

loki:
  enabled: false
